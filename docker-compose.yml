version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: holmes_backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=sqlite+aiosqlite:////data/holmes.db
      - DATABASE_PATH=/app/data/holmes.db
      - HOLMES_SECRET_KEY=holmes-default-secret-key-32bytes-long!
    volumes:
      - holmes_data:/data
      - holmes_data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: holmes_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=
    restart: unless-stopped

volumes:
  holmes_data:
    driver: local
